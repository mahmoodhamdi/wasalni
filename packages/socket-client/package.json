{
  "name": "@wasalni/socket-client",
  "version": "0.1.0",
  "private": true,
  "description": "Typed wrapper around socket.io-client that mirrors the backend event map.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./events": "./src/events.ts"
  },
  "scripts": {
    "lint": "echo 'lint runs at app boundary' && exit 0",
    "typecheck": "tsc --noEmit",
    "test": "echo 'tests added in PR 21' && exit 0"
  },
  "dependencies": {
    "@wasalni/shared-types": "workspace:*",
    "socket.io-client": "^4.8.3"
  },
  "devDependencies": {
    "@wasalni/config-tsconfig": "workspace:*",
    "typescript": "^5.7.3"
  }
}
