{
  "name": "@wasalni/pwa",
  "version": "0.1.0",
  "private": true,
  "description": "PWA primitives — service-worker registration helpers, install prompt, FCM push hook, Wake Lock hook. Real content lands in PR 6 (passenger) and PR 16 (driver).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "echo 'lint runs at app boundary' && exit 0",
    "typecheck": "tsc --noEmit",
    "test": "echo 'tests added in PR 21' && exit 0"
  },
  "devDependencies": {
    "@wasalni/config-tsconfig": "workspace:*",
    "typescript": "^5.7.3"
  }
}
