/**
 * Placeholder. Service-worker helpers (install/update lifecycle), Wake Lock
 * hook, FCM push subscription helper, and install-prompt primitives land in
 * PR 6 (passenger PWA shell) and PR 16 (driver PWA shell + Wake Lock).
 */
export {};
