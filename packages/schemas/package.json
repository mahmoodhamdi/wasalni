{
  "name": "@wasalni/schemas",
  "version": "0.1.0",
  "private": true,
  "description": "Zod runtime schemas mirroring @wasalni/shared-types domain. Used by client forms and Route Handler boundary validation.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "echo 'lint runs at app boundary' && exit 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@wasalni/shared-types": "workspace:*",
    "@wasalni/utils": "workspace:*",
    "zod": "^4.3.4"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "@wasalni/config-tsconfig": "workspace:*",
    "@wasalni/config-vitest": "workspace:*",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
