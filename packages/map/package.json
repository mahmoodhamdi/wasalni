{
  "name": "@wasalni/map",
  "version": "0.1.0",
  "private": true,
  "description": "MapLibre GL JS wrappers + place autocomplete + route rendering. Real content lands in PR 7.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "echo 'lint runs at app boundary' && exit 0",
    "typecheck": "tsc --noEmit",
    "test": "echo 'tests added in PR 21' && exit 0"
  },
  "devDependencies": {
    "@wasalni/config-tsconfig": "workspace:*",
    "typescript": "^5.7.3"
  }
}
