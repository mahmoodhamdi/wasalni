# `apps/`

End‑user applications.

| Path             | Description                | Status          |
| ---------------- | -------------------------- | --------------- |
| `passenger-web/` | Passenger PWA (Next.js 16) | Planned — PR 4  |
| `driver-web/`    | Driver PWA (Next.js 16)    | Planned — PR 14 |

`admin-dashboard/` and `backend/` remain at the repo root for now and join the
workspace via `pnpm-workspace.yaml`. They are not moved to keep individual PRs
small. See [docs/web-migration/PLAN.md](../docs/web-migration/PLAN.md).
