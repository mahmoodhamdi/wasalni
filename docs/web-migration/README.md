# Web Migration

This directory captures the historical migration to two installable
Progressive Web Apps (`apps/passenger-web` and `apps/driver-web`).

| Document             | Purpose                                                         |
| -------------------- | --------------------------------------------------------------- |
| [PLAN.md](./PLAN.md) | Single source of truth — architecture, decisions, 28‑PR roadmap |

The legacy native mobile apps have been removed from the repo. Their full
source is still available in git history if you ever need to consult it.
